image:
  name: ruby:3.1
  run-as-user: 1000

pipelines:
  default:
    - step:
        name: specs
        script:
          - apt-get update -q && apt-get install -yqq nodejs && apt-get install -yqq chromium
          - bundle install
          - bundle exec rake
